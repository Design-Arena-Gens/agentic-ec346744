export const metadata = {
  title: "Speaker Numbers",
  description: "Click speaker to animate numbers green",
};

import "./globals.css";
import { ReactNode } from "react";

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
